BCD counter is a 4-bit counter that counts from 0 to 9. The "count"
increases at every positive edge of the "clock" until it reaches 9. 
Then, it resets to 0. "reset" initializes the "count" to 0.

  
